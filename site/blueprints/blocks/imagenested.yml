name: Image (nested)
icon: image
preview: image
fields:
  image:
    label: field.blocks.image.name
    type: files
    query: model.images
    multiple: false
    image:
      back: black
    uploads:
      template: blocks/image

  link:
    label: field.blocks.image.link
    type: text
    icon: url
