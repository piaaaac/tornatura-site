title: Articles index

tabs:
  content:
    columns:
      main:
        sections:
          fields2:
            type: fields
            fields:
              showTemplateName:
                extends: fields/showTemplate
          pages:
            type: pages
            create: article
            sortBy: date desc

  seo: seo/page
