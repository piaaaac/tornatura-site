title: Instructions

tabs:
  content:
    columns:
      main:
        width: 2/3
        sections:
          fields:
            type: fields
            fields:
              blocks:
                extends: fields/block-field
      sidebar:
        width: 1/3
        sections:
          fields2:
            type: fields
            fields:
              showTemplateName:
                extends: fields/showTemplate
          pages: false
          files:
            type: files

  seo: seo/page
